# This is a Declarative Automation Bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: 01b66bc9-5aa2-4ca2-9196-ab6822c9b301

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: adb-7405610531028829.9.azuredatabricks.net
    presets:
      source_linked_deployment : false # it is for devlopment enviornment so need to remove source artifacts
     
  prod:
    mode: production
    workspace:
      host: adb-7405610531028829.9.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/honeysurti@hotmail.com to make sure we only have a single copy.
      root_path: /Workspace/prod/.bundle/${bundle.name}/${bundle.target}
     #variables:
      #catalog: databricksazureproject
      #schema: prod 
    permissions:
      - user_name: honeysurti@hotmail.com
        level: CAN_MANAGE
